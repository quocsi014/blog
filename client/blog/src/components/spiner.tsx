export const Spinner = ({className}: {className?: string})=>{
  return (
    <div className={`${className} animate-spin size-4 bg-transparent border-2 border-gray-200 border-t-transparent rounded-full `}>
    </div>
  )
} 